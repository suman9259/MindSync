package com.example.mindsync.presentation.theme

import androidx.compose.ui.graphics.Color

// Material Teal Color Palette for Dark Theme
// Primary - Material Teal
val TealPrimary = Color(0xFF03DAC5)       // Material Teal primary
val TealLight = Color(0xFF64FFDA)         // Lighter teal
val TealDark = Color(0xFF00C2A8)          // Darker teal
val PrimaryBlue = Color(0xFF03DAC5)       // Material Teal
val PrimaryLight = Color(0xFF03DAC5)
val PrimaryDark = Color(0xFF03DAC5)
val PrimaryGradientStart = Color(0xFF03DAC5)  // Teal
val PrimaryGradientEnd = Color(0xFF64FFDA)    // Light teal

// Secondary colors - Complementary to teal
val SecondaryLight = Color(0xFFBB86FC)    // Material Purple (complements teal)
val SecondaryDark = Color(0xFFBB86FC)
val AccentYellow = Color(0xFFFFD54F)      // Amber accent

// Tertiary colors for variety
val PastelMint = Color(0xFF03DAC5)        // Teal (for theme compatibility)
val TealVariant = Color(0xFF018786)       // Dark teal variant
val PurpleAccent = Color(0xFFBB86FC)      // Material Purple
val CoralAccent = Color(0xFFCF6679)       // Material Error/Coral
val AmberAccent = Color(0xFFFFD54F)       // Amber
val CyanAccent = Color(0xFF84FFFF)        // Cyan accent

// Background colors - Pure dark theme
val BackgroundLight = Color(0xFF121212)   // Material Dark background
val BackgroundDark = Color(0xFF121212)

// Surface colors - Material Dark surfaces
val SurfaceLight = Color(0xFF1E1E1E)      // Material Dark surface
val SurfaceDark = Color(0xFF1E1E1E)
val SurfaceVariantDark = Color(0xFF2D2D2D)  // Elevated surface

// Text colors
val TextPrimaryLight = Color(0xFFFFFFFF)  // Pure white
val TextPrimaryDark = Color(0xFFFFFFFF)
val TextSecondaryLight = Color(0xFFB3B3B3)  // Gray
val TextSecondaryDark = Color(0xFFB3B3B3)

// Status colors
val SuccessGreen = Color(0xFF03DAC5)      // Teal for success
val ErrorRed = Color(0xFFCF6679)          // Material Error
val WarningYellow = Color(0xFFFFD54F)     // Amber warning

// Component specific colors
val CardBackgroundLight = Color(0xFF1E1E1E)
val CardBackgroundDark = Color(0xFF1E1E1E)
val DividerColor = Color(0xFF3D3D3D)

// Progress colors - Teal gradient
val ProgressStart = Color(0xFF03DAC5)     // Teal
val ProgressEnd = Color(0xFF64FFDA)       // Light teal

// Workout specific colors
val WorkoutBlue = Color(0xFF03DAC5)       // Teal
val WorkoutPurple = Color(0xFFBB86FC)     // Purple

// Feature-specific colors (all using teal variants)
val MeditationColor = Color(0xFFBB86FC)   // Purple for meditation
val SkincareColor = Color(0xFF03DAC5)     // Teal for skincare
val GroceryColor = Color(0xFF03DAC5)      // Teal for grocery
val MedicineColor = Color(0xFF03DAC5)     // Teal for medicine
val ReminderColor = Color(0xFF03DAC5)     // Teal for reminders
val FamilyColor = Color(0xFFBB86FC)       // Purple for family